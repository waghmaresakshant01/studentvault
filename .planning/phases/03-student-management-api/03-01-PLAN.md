---
phase: 03-student-management-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [models/Student.js, middleware/isAdmin.js, controllers/studentController.js, routes/studentRoutes.js, server.js]
autonomous: true
requirements: [STUD-01, STUD-02, STUD-03, STUD-04, STUD-05, STUD-06, STUD-07]

must_haves:
  truths:
    - "Only admin users can create, update, or delete student records; standard student users are denied access to these write operations."
    - "All authenticated users (both admin and student roles) can retrieve student records (GET /api/students and GET /api/students/:id)."
    - "Student database validation enforces unique roll number, email regex, 10-digit phone regex, and academic year between 1 and 4."
  artifacts:
    - path: "models/Student.js"
      provides: "MongoDB Student model and validation rules"
    - path: "middleware/isAdmin.js"
      provides: "Admin authorization check middleware"
    - path: "controllers/studentController.js"
      provides: "Student CRUD controller logic"
    - path: "routes/studentRoutes.js"
      provides: "Student REST endpoint routing"
---

<objective>
Implement the Student Mongoose Model with unique rollNo and format validations, create the isAdmin role check middleware, build the Student REST CRUD controller and routes, and mount them in server.js protected by authentication.

Purpose: Business logic and API gates for student ERP data records.
Output: Student model, admin middleware, student controller, student routes, and updated server entry.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-student-management-api/03-CONTEXT.md
@middleware/auth.js
@server.js
</context>

<tasks>
<task type="auto">
  <name>Create Student MongoDB Model</name>
  <files>models/Student.js</files>
  <action>Create models/Student.js. Schema fields: name (String, required), rollNo (String, required, unique, trim), branch (String, required, trim), year (Number, required, min: 1, max: 4), email (String, required, lowercase, trim, regex matching standard email), phone (String, required, trim, regex matching exactly 10 digits). Export Mongoose model. Follow D-03, D-04, D-05, D-06, D-07.</action>
  <verify>node -e "require('./models/Student')"</verify>
  <done>Student model is defined with correct validations and successfully loaded.</done>
</task>

<task type="auto">
  <name>Create Admin Authorization Middleware</name>
  <files>middleware/isAdmin.js</files>
  <action>Create middleware/isAdmin.js. Check if req.user exists and req.user.role === 'admin'. If so, call next(). If not, return 403 Forbidden with standard format { success: false, message: "Access denied. Admin role required." }. Export middleware. Follow D-01.</action>
  <verify>node -e "require('./middleware/isAdmin')"</verify>
  <done>Admin authorization middleware created and syntax check passes.</done>
</task>

<task type="auto">
  <name>Create Student CRUD Controller</name>
  <files>controllers/studentController.js</files>
  <action>Create controllers/studentController.js. Implement createStudent (validate incoming fields, check if rollNo exists, save to MongoDB, return 201), getAllStudents (retrieve all records, return 200), getStudentById (retrieve by ID, check existence, return 200/404), updateStudent (retrieve by ID, update fields, validate unique rollNo on conflict, return 200/404), and deleteStudent (retrieve by ID, hard delete from MongoDB, return 200/404). All responses must follow standard JSON response format. Follow D-02, D-03, D-08.</action>
  <verify>node -e "require('./controllers/studentController')"</verify>
  <done>Student controller contains CRUD actions satisfying spec requirements.</done>
</task>

<task type="auto">
  <name>Create Student Routes and Mount in server.js</name>
  <files>routes/studentRoutes.js, server.js</files>
  <action>Create routes/studentRoutes.js. Map POST / (auth + isAdmin -> createStudent), GET / (auth -> getAllStudents), GET /:id (auth -> getStudentById), PUT /:id (auth + isAdmin -> updateStudent), DELETE /:id (auth + isAdmin -> deleteStudent). In server.js, require routes and mount via app.use('/api/students', studentRoutes). Follow D-01, D-02, D-07.</action>
  <verify>node --check server.js</verify>
  <done>Student routes are registered and server successfully compiles.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Student API | Untrusted input crossing from clients (tokens, request bodies) to Node.js backend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | Student routes | mitigate | Restrict POST, PUT, DELETE routes with the isAdmin middleware, checking req.user.role. |
| T-03-02 | Spoofing | Student routes | mitigate | Protect all routes under /api/students using the JWT authentication middleware. |
| T-03-03 | Information Disclosure | studentController | mitigate | Use try/catch blocks to intercept Mongoose validation/connection errors and return clean error messages, preventing database stack trace leaks. |
| T-03-04 | Tampering | Student.js | mitigate | Mongoose schema-level regex validation for email/phone and range checks for year to block invalid data inputs. |
</threat_model>

<verification>
- Verify server runs and imports all modules cleanly.
</verification>

<success_criteria>
- Student model, admin middleware, controller, and routes are implemented.
- Express registers the endpoints under /api/students.
</success_criteria>

<output>
After completion, create `.planning/phases/03-student-management-api/03-01-SUMMARY.md`
</output>
