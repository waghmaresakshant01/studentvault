---
phase: 01-core-backend-crud-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config/db.js, models/Student.js, controllers/studentController.js, routes/studentRoutes.js, server.js, .env, .gitignore, package.json]
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05, API-08]

must_haves:
  truths:
    - "Server successfully initializes and connects to MongoDB via Mongoose."
    - "Endpoints POST, GET, GET by ID, PUT, and DELETE on /api/students correctly manage records in MongoDB."
    - "All responses conform to the standard JSON body envelope: success, message, and data fields."
  artifacts:
    - path: "config/db.js"
      provides: "Mongoose database connection"
    - path: "models/Student.js"
      provides: "Student model schema definition"
    - path: "controllers/studentController.js"
      provides: "CRUD request handlers"
    - path: "routes/studentRoutes.js"
      provides: "API routes configuration"
    - path: "server.js"
      provides: "Server bootstrapper, static serving, and middleware wiring"
---

<objective>
Setup the core StudentVault REST API by initializing the MongoDB connection, creating the Student model, building CRUD controllers, and configuring the Express server with CORS and static folder serving.

Purpose: Backend REST API engine for StudentVault records.
Output: DB config, Mongoose model, CRUD controller, routes, and bootstrapped server.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-backend-crud-api/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Configure Database Connection and Environment</name>
  <files>config/db.js, .env, .gitignore</files>
  <action>Create .env specifying PORT=5001 and MONGO_URI=mongodb://localhost:27017/studentvault. Create .gitignore ignoring node_modules and .env. Create config/db.js importing mongoose and connecting to MONGO_URI, log connection status. Follow D-03.</action>
  <verify>node -e "require('dotenv').config(); require('./config/db')"</verify>
  <done>Database connection script loads successfully and establishes connection.</done>
</task>

<task type="auto">
  <name>Create Student Schema Model</name>
  <files>models/Student.js</files>
  <action>Create models/Student.js. Fields: name (String, required), rollNo (String, required, unique, trim), branch (String, required), year (Number, required), email (String, required, unique, lowercase, trim), phone (String, required), address (String, optional), createdAt (Date, default: Date.now). Follow D-04, D-05.</action>
  <verify>node -e "require('./models/Student')"</verify>
  <done>Student model schema defined and loads correctly.</done>
</task>

<task type="auto">
  <name>Implement Student CRUD Controller</name>
  <files>controllers/studentController.js</files>
  <action>Create controllers/studentController.js. Implement createStudent (POST, returns 201), getAllStudents (GET, returns 200), getStudentById (GET, returns 200/404), updateStudent (PUT, returns 200/404), and deleteStudent (DELETE, returns 200/404). Return standard JSON body format { success: true, message: "...", data: {...} } or { success: false, message: "..." }. Handle CastError IDs elegantly. Follow D-06, D-07.</action>
  <verify>node -e "require('./controllers/studentController')"</verify>
  <done>Student controllers implemented for all five CRUD operations.</done>
</task>

<task type="auto">
  <name>Configure Express Server, CORS, and Routes</name>
  <files>routes/studentRoutes.js, server.js, package.json</files>
  <action>Install cors dependency: npm install cors. Create routes/studentRoutes.js mapping routes: POST /, GET /, GET /:id, PUT /:id, DELETE /:id. In server.js: import db, express, cors, and studentRoutes. Enable cors(), express.json(), serve 'public' directory statically: app.use(express.static('public')), mount studentRoutes under /api/students, and listen on PORT. Follow D-01, D-02.</action>
  <verify>node --check server.js</verify>
  <done>Express server configuration and router mapping verified.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted JSON request payloads crossing into backend environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Endpoints | accept | Authentication is Out of Scope for v1 milestone per project requirements. |
| T-01-02 | Information Disclosure | studentController | mitigate | Wrap ObjectId lookups in try/catch to intercept CastError/validation stack trace leaks and return standard 404 responses. |
| T-01-03 | Tampering | server.js | mitigate | Wire Express CORS middleware globally to restrict or allow expected origin request headers. |
</threat_model>

<verification>
- Ensure server compiles and launches correctly.
</verification>

<success_criteria>
- db.js, Student.js, studentController.js, studentRoutes.js, and server.js created and integrated.
- Server starts cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-backend-crud-api/01-01-SUMMARY.md`
</output>
