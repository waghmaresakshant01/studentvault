---
phase: 04-frontend-modals-filtering-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/index.html, public/style.css, public/app.js]
autonomous: true
requirements: [FE-07, FE-08, FE-09, FE-10, FE-11, FE-12]

must_haves:
  truths:
    - "Client-side live search filters table rows instantly by name/rollNo without calling the API."
    - "Branch and year dropdown filters call the respective backend filter routes and render results."
    - "Clicking 'Clear Filters' resets all filter criteria and loads all student records."
    - "Clicking Edit opens a pre-filled glassmorphic modal and saving updates the student record via PUT /api/students/:id."
    - "Clicking Delete displays a premium confirmation modal, deleting the student on confirm via DELETE /api/students/:id."
    - "Stats cards displaying Total, CSE, IT, and ECE student counts update in real-time."
  artifacts:
    - path: "public/index.html"
      provides: "modal structures, search bars, filter dropdowns, and stats counter boxes"
    - path: "public/style.css"
      provides: "animations and styling rules for modals, inputs, and stats layout"
    - path: "public/app.js"
      provides: "dynamic interaction handlers, PUT/DELETE fetch binding, search listeners, and count calculators"
---

<objective>
Enhance the StudentVault UI by adding edit modals, confirmation dialogs, client-side live search, branch/year filter selections, and real-time statistics counters.

Purpose: Deliver complete interactive CRUD functionality and database queries in the user interface.
Output: public/index.html, public/style.css, public/app.js.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@public/index.html
@public/style.css
@public/app.js
</context>

<tasks>
<task type="auto">
  <name>Extend HTML for modals, search, filters, and stats</name>
  <files>public/index.html</files>
  <read_first>public/index.html</read_first>
  <action>Update public/index.html. 1. Add a live search bar and filter controls (branch select, year select, clear filters button) above the student directory table. 2. Add a statistics bar deck showing cards for: Total Students, CSE Students, IT Students, ECE Students. 3. Add edit modal overlay structure and delete confirmation modal overlay structure at the bottom of the body. Assign unique IDs to all inputs, modals, and buttons. Follow FE-07, FE-08, FE-09, FE-10, FE-11, FE-12.</action>
  <verify>grep -q "editModal" public/index.html && grep -q "searchInput" public/index.html</verify>
  <done>public/index.html contains all necessary modal overlays, search controls, and stats bar grids.</done>
</task>

<task type="auto">
  <name>Style modals, inputs, and stats bar cards</name>
  <files>public/style.css</files>
  <read_first>public/style.css</read_first>
  <action>Update public/style.css. Add layout styling for the stats deck cards (glow accents, numbers, icons). Add control row grids for search bar and filters. Add styles for modal overlays (dark translucent backdrop, backdrop blur, sliding modal card, cancel and confirm/save button variations). Add hover and focus rings. Follow FE-07, var properties, modal visual states, and FE-12.</action>
  <verify>grep -q "modal-overlay" public/style.css && grep -q "stats-grid" public/style.css</verify>
  <done>public/style.css contains complete styles for stats widgets and modal overlays.</done>
</task>

<task type="auto">
  <name>Implement edit/delete modals, filter API queries, and stats counters</name>
  <files>public/app.js</files>
  <read_first>public/app.js</read_first>
  <action>Update public/app.js. 1. Implement live search (keyup listener on search bar filtering visible table rows). 2. Bind change listeners to branch/year dropdowns querying the respective GET routes and rendering results. 3. Implement Clear Filters button (resets dropdowns/search and fetches all). 4. Implement Edit button click to open pre-filled edit modal and submit PUT request on save. 5. Implement Delete button to open custom confirm modal and submit DELETE request on approval. 6. Recalculate stats counters (Total, CSE, IT, ECE counts) dynamically after any load, creation, update, or deletion. Follow FE-07, FE-08, FE-09, FE-10, FE-11, FE-12.</action>
  <verify>grep -q "editModal" public/app.js && grep -q "delete" public/app.js</verify>
  <done>public/app.js is fully wired with search, filter, modal, and stats update logic.</done>
</task>
</tasks>

<verification>
- Start the server and confirm all CRUD and filter operations work seamlessly.
- Verify stats counters update automatically.
</verification>

<success_criteria>
- Modals function cleanly with transitions.
- Client-side search filters rows instantly.
- Dropdown filters update the directory with data from the database.
- Statistics report correct values at all times.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-modals-filtering-stats/04-01-SUMMARY.md`
</output>
