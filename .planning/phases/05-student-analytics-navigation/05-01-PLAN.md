---
phase: 05-student-analytics-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/index.html, public/style.css, public/app.js]
autonomous: true
requirements: [FE-13, FE-14, FE-15, FE-16]

must_haves:
  truths:
    - "Clicking Navbar tabs switches active view sections smoothly without full page refresh."
    - "Dashboard tab includes quick actions, recent additions, and basic status overviews."
    - "Students tab correctly houses the filterable, full-width directory table."
    - "Analytics tab embeds dynamic bar and pie charts rendering branch/year data via Chart.js."
  artifacts:
    - path: "public/index.html"
      provides: "view section containers, quick actions, canvas containers for Chart.js, and CDN script inclusion"
    - path: "public/style.css"
      provides: "tab transition animations, quick action grids, and analytics container layouts"
    - path: "public/app.js"
      provides: "client-side router, recent student list updates, Chart.js compilation, and rendering loops"
---

<objective>
Build a multi-view SPA navigation system inside StudentVault. Split the dashboard into three dedicated sections (Dashboard Home, Students Directory, and Analytics Visualizations) toggled via client-side routing. Integrate interactive Chart.js charts.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@public/index.html
@public/style.css
@public/app.js
</context>

<tasks>
<task type="auto">
  <name>Restructure index.html markup into view sections and add Chart.js</name>
  <files>public/index.html</files>
  <read_first>public/index.html</read_first>
  <action>Update public/index.html. 1. Add Chart.js CDN script tag in head. 2. Wrap content inside main.main-content into three distinct sections: section#dashboardView (holds Hero Header, stats deck, a new 'Recent Registrations' list container, and a 'Quick Actions' grid), section#studentsView (holds filter-bar and data table), and section#analyticsView (holds chart cards with canvases for branchChart and yearChart). 3. Add href links for '#dashboard', '#students', '#analytics' on the top-nav navbar links. Follow FE-13, FE-14, FE-15, FE-16.</action>
  <verify>grep -q "dashboardView" public/index.html && grep -q "analyticsView" public/index.html && grep -q "chart.js" public/index.html</verify>
  <done>public/index.html structure has three view sections, quick actions, chart container canvases, and top nav hash hrefs.</done>
</task>

<task type="auto">
  <name>Style view transitions, quick actions, and chart containers</name>
  <files>public/style.css</files>
  <read_first>public/style.css</read_first>
  <action>Update public/style.css. 1. Define .view-section transition rules (hidden by default, active visible via keyframe fade-in). 2. Add layout grids and responsive column sets for Dashboard Quick Action cards (hover animations, icons) and Recent Registrations panel. 3. Add styling for Analytics view grid containing two large glassmorphic cards for the charts. Follow FE-13, FE-14, FE-15, FE-16.</action>
  <verify>grep -q "view-section" public/style.css && grep -q "analytics-grid" public/style.css</verify>
  <done>public/style.css contains complete styles for tab switching, dashboard shortcuts, and analytics widgets.</done>
</task>

<task type="auto">
  <name>Implement Vanilla JS Tab Router, Recent list, and Chart.js integration</name>
  <files>public/app.js</files>
  <read_first>public/app.js</read_first>
  <action>Update public/app.js. 1. Add route controller logic: listen to 'hashchange' and toggle active classes on view sections and navbar links. Fall back to '#dashboard' if no hash exists. 2. Update fetchStudents to extract the last 3 registered students and render them inside the Dashboard's Recent Registrations panel. 3. Compile student records by branch and year, and instantiate or update Chart.js instances (bar chart for Branch Distribution, doughnut/pie chart for Year-wise Enrollment) inside the Analytics view. Custom style the chart palettes to align with dark theme orange accents. Follow FE-13, FE-14, FE-15, FE-16.</action>
  <verify>grep -q "hashchange" public/app.js && grep -q "Chart" public/app.js</verify>
  <done>public/app.js executes client routing, renders recent registries, and compiles and draws live charts.</done>
</task>
</tasks>

<verification>
- Start node server and navigate to dashboard.
- Verify tab-switching occurs on navbar link clicks, updating hash.
- Add/edit/delete students and verify changes immediately propagate to the stats bar, Dashboard recent records list, and Analytics Chart.js graphs.
</verification>

<success_criteria>
- Navigation changes tabs smoothly without page refresh.
- Recent list shows the correct latest 3 records.
- Analytics tab renders charts that match visual styling guidelines.
</success_criteria>

<output>
After completion, create `.planning/phases/05-student-analytics-navigation/05-01-SUMMARY.md`
</output>
