---
phase: 02-authentication-middleware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [models/User.js, middleware/auth.js, controllers/authController.js, routes/authRoutes.js, server.js]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]

must_haves:
  truths:
    - "User registration hashes password, requires email/password/name, validates email format and min 6 password characters"
    - "User login compares password via bcrypt and returns a valid 7-day JWT token on success"
    - "GET /api/auth/profile route returns authenticated user data when accessed with valid Bearer token"
    - "Middleware blocks unauthorized traffic with a 401 status code"
  artifacts:
    - path: "models/User.js"
      provides: "MongoDB User schema model definition"
    - path: "middleware/auth.js"
      provides: "JWT extraction and verification middleware"
    - path: "controllers/authController.js"
      provides: "Register and login endpoint controller actions"
    - path: "routes/authRoutes.js"
      provides: "Auth routes setup"
---

<objective>
Build the User Model, Auth Middleware, Auth Controllers, and Register/Login/Profile Routes. Mount them in server.js.

Purpose: Core security gate for user credentials and API route authorization.
Output: User model file, auth middleware file, auth controller file, auth routes file, and updated server entry.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@config/mongodb.js
@server.js
</context>

<tasks>
<task type="auto">
  <name>Create User MongoDB Model</name>
  <files>models/User.js</files>
  <action>Create models/User.js. Schema fields: name (String, required), email (String, required, unique, lowercase, trimmed), password (String, required), role (String, enum: ['admin', 'student'], default: 'student'), createdAt (Date, default: Date.now). Export model.</action>
  <verify>node -e "require('./models/User')"</verify>
  <done>User mongoose model defined and loads correctly.</done>
</task>

<task type="auto">
  <name>Create JWT Auth Middleware</name>
  <files>middleware/auth.js</files>
  <action>Create middleware/auth.js. Check headers.authorization for Bearer token. If missing or invalid, return 401. If valid, decode JWT and attach user object to req.user. Export middleware.</action>
  <verify>node -e "require('./middleware/auth')"</verify>
  <done>Auth middleware written and syntax check passes.</done>
</task>

<task type="auto">
  <name>Create Auth Controller</name>
  <files>controllers/authController.js</files>
  <action>Create controllers/authController.js. Implement register (validate fields, bcrypt.hash password, save User, return success response) and login (find User by email, bcrypt.compare password, sign JWT with secret expiring in 7d, return success response with token). Implement profile returning req.user details. Follow standard response format `{ success: true, message: "...", data: {...} }`.</action>
  <verify>node -e "require('./controllers/authController')"</verify>
  <done>Auth controller contains register, login, and profile logics.</done>
</task>

<task type="auto">
  <name>Create Auth Routes and Mount in server.js</name>
  <files>routes/authRoutes.js, server.js</files>
  <action>Create routes/authRoutes.js. Map POST /register, POST /login, and GET /profile (using auth middleware). Mount routes under app.use('/api/auth', authRoutes) in server.js.</action>
  <verify>node --check server.js</verify>
  <done>Routes are mounted and server compiles.</done>
</task>
</tasks>

<verification>
- [ ] Server syntax is correct
</verification>

<success_criteria>
- Auth files are created
- Express router registers /api/auth routes
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-middleware/02-01-SUMMARY.md`
</output>
