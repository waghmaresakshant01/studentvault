---
phase: 03-single-page-frontend-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/index.html, public/style.css, public/app.js]
autonomous: true
requirements: [FE-01, FE-02, FE-03, FE-04, FE-05, FE-06]

must_haves:
  truths:
    - "Express serves index.html on root '/' access."
    - "Add Student Form successfully adds a student via POST /api/students and clears the form inputs."
    - "Students table displays current database records dynamically on page load."
    - "Toast notifications display success or error messages that auto-dismiss after 3 seconds."
  artifacts:
    - path: "public/index.html"
      provides: "SPA layout structure for StudentVault"
    - path: "public/style.css"
      provides: "premium dark-navy glassmorphic styling system"
    - path: "public/app.js"
      provides: "dynamic data fetching, rendering, and toast alerts"
---

<objective>
Configure static folder serving, construct the premium UI layout (Navbar, Card Form, Responsive Table) with CSS glassmorphism, and write Vanilla JS logic to fetch students, submit the form, and show beautiful 3-second toast notifications.

Purpose: Provide an attractive and interactive user interface for core student CRUD operations.
Output: public/index.html, public/style.css, public/app.js.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server.js
@routes/studentRoutes.js
@controllers/studentController.js
</context>

<tasks>
<task type="auto">
  <name>Create HTML layout structure</name>
  <files>public/index.html</files>
  <action>Create the public/ directory. Create public/index.html. Include Outfit Google Font. Create a premium dark theme container: Header with title StudentVault and tagline, Add Student Form section with branch/year dropdown options (CSE, IT, ECE, ME, CE and 1, 2, 3, 4), Students Table with action placeholders, and toast notifications container. Follow SEO best practices and ensure all interactive elements have unique IDs. Follow FE-01, FE-02, FE-03, FE-05.</action>
  <verify>test -f public/index.html</verify>
  <done>public/index.html is created with proper semantic HTML structures and IDs.</done>
</task>

<task type="auto">
  <name>Create premium styling sheet</name>
  <files>public/style.css</files>
  <action>Create public/style.css. Define a modern, rich aesthetic system (dark navy background, glassmorphism cards with translucent borders, smooth gradients, premium neon-glowing active accents, clean focus rings). Style the Add Student form, grid layout, responsive table with colored tag badges for branches, and floating toast notifications with slide-in/fade transitions. Follow FE-02, FE-03, FE-04, FE-05.</action>
  <verify>test -f public/style.css</verify>
  <done>public/style.css defines the design tokens and layout styling system cleanly.</done>
</task>

<task type="auto">
  <name>Implement Vanilla JS API fetch and interaction logic</name>
  <files>public/app.js</files>
  <action>Create public/app.js. Write logic to: 1. Fetch student list from GET /api/students and render rows dynamically. 2. Handle form submission (POST /api/students) with fetch, parse potential 400 error arrays to show error toasts, or show a green success toast on 201 and reload table. 3. Display auto-dismissing toast messages (dismiss after 3s). Update total student count label. Follow FE-04, FE-05, FE-06.</action>
  <verify>test -f public/app.js</verify>
  <done>public/app.js successfully handles form submission, fetching, rendering, and toast logic.</done>
</task>
</tasks>

<verification>
- Verify that server serves static public files correctly on port 5001.
- Verify form submission successfully saves to MongoDB and updates the UI table.
</verification>

<success_criteria>
- index.html, style.css, app.js created.
- Form submissions work without full page reload.
- Beautiful toast alerts render on success/failure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-single-page-frontend-interface/03-01-SUMMARY.md`
</output>
