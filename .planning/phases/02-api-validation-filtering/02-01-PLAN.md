---
phase: 02-api-validation-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [middleware/validateStudent.js, controllers/studentController.js, routes/studentRoutes.js, server.js, package.json]
autonomous: true
requirements: [VAL-01, VAL-02, VAL-03, VAL-04, VAL-05, VAL-06, API-06, API-07]

must_haves:
  truths:
    - "Student creation endpoint POST /api/students returns 400 Bad Request with an array of detailed error objects if validation fails."
    - "Filtering by branch (GET /api/students/branch/:branch) and year (GET /api/students/year/:year) retrieves records matching the filter case-insensitively."
    - "Uniqueness constraints for email and rollNo are validated at the middleware level on POST, and controller level on PUT."
  artifacts:
    - path: "middleware/validateStudent.js"
      provides: "express-validator middleware configurations"
---

<objective>
Implement input validation middleware for student creation using express-validator, implement branch and year case-insensitive filtering routes, and integrate validation logic in the controllers and routers.

Purpose: Data sanitization, query filters, and database record integrity.
Output: Validation middleware script, updated CRUD/filter controllers, and updated router.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-validation-filtering/02-CONTEXT.md
@models/Student.js
@routes/studentRoutes.js
@controllers/studentController.js
@server.js
</context>

<tasks>
<task type="auto">
  <name>Install express-validator and Create Validation Middleware</name>
  <files>middleware/validateStudent.js, package.json</files>
  <action>Install express-validator: npm install express-validator. Create middleware/validateStudent.js. Define validation rules using express-validator for: name (required), rollNo (required, must not exist in database), branch (required, must be one of CSE, IT, ECE, ME, CE), year (required, integer 1-4), email (required, valid format, must not exist in database), phone (required, exactly 10 digits). Format error response to return array of objects: { errors: [ { field: param, message: msg } ] } with status 400. Follow D-01, D-02, D-03.</action>
  <verify>node -e "require('express-validator'); require('./middleware/validateStudent')"</verify>
  <done>express-validator is installed and validateStudent middleware is defined successfully.</done>
</task>

<task type="auto">
  <name>Implement API Filtering and Update Controller CRUD Actions</name>
  <files>controllers/studentController.js</files>
  <action>Update controllers/studentController.js. Implement getStudentsByBranch (case-insensitive find branch, return 200) and getStudentsByYear (find year, return 200). Update updateStudent to validate uniqueness of email and rollNo if they are modified, returning 400 on conflict. Follow D-04, D-05, D-06.</action>
  <verify>node -e "require('./controllers/studentController')"</verify>
  <done>Student controller updated with filter routes and unique constraint update checks.</done>
</task>

<task type="auto">
  <name>Map Validation Middleware and Filter Routes</name>
  <files>routes/studentRoutes.js, server.js</files>
  <action>Update routes/studentRoutes.js. Import validateStudent. Bind validateStudent to POST /. Map routes: GET /branch/:branch (getStudentsByBranch) and GET /year/:year (getStudentsByYear). Verify server.js compiles successfully. Follow D-04, D-05, D-07.</action>
  <verify>node --check server.js</verify>
  <done>Express routes configured and server compiles cleanly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted input validation checks crossed at middleware entry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | validateStudent | mitigate | Strict input validation rules using express-validator to enforce schema structures. |
| T-02-02 | Information Disclosure | studentController | mitigate | Prevent database duplicate key trace leakage by catching errors and returning standard error JSON responses. |
| T-02-03 | Denial of Service | studentController | mitigate | Case-insensitive sanitization on query strings to prevent unhandled database regex match crashes. |
</threat_model>

<verification>
- Ensure server compiles and launches correctly.
</verification>

<success_criteria>
- validateStudent.js created.
- CRUD and filter routes compile.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-validation-filtering/02-01-SUMMARY.md`
</output>
