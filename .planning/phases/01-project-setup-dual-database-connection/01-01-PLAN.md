---
phase: 01-project-setup-dual-database-connection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .env, .gitignore, config/mongodb.js, config/sqlite.js, server.js]
autonomous: true
requirements: [DB-01, DB-02, LOGG-01, DOCS-01]

must_haves:
  truths:
    - "Express server starts successfully and connects to MongoDB and SQLite"
    - "SQLite auditlog.db file and audit_logs table are automatically created on server start"
  artifacts:
    - path: "config/mongodb.js"
      provides: "MongoDB connection setup"
    - path: "config/sqlite.js"
      provides: "SQLite connection setup and table initialization"
    - path: "server.js"
      provides: "Express server entry point"
---

<objective>
Configure the basic project, setup environment variables, initialize gitignore, configure MongoDB (Mongoose) connection, and initialize the SQLite database table.

Purpose: Foundation for dual-database API hits and student records management.
Output: Initial boilerplate structure, database configuration files, and running server.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Initialize npm project and install dependencies</name>
  <files>package.json</files>
  <action>Run npm init -y, and install better-sqlite3, mongoose, express, bcrypt, jsonwebtoken, and dotenv.</action>
  <verify>npm list mongoose && npm list better-sqlite3</verify>
  <done>package.json has dependencies recorded and node_modules installed.</done>
</task>

<task type="auto">
  <name>Create config files (.env and .gitignore)</name>
  <files>.env, .gitignore</files>
  <action>Create .env with variables PORT=5000, MONGO_URI=mongodb://localhost:27017/campuscore, and JWT_SECRET=campuscore_secret_key_2026. Create .gitignore ignoring node_modules/, .env, and database/auditlog.db.</action>
  <verify>test -f .env && test -f .gitignore</verify>
  <done>.env and .gitignore exist with correct values.</done>
</task>

<task type="auto">
  <name>Implement MongoDB and SQLite configuration connections</name>
  <files>config/mongodb.js, config/sqlite.js</files>
  <action>Create config/mongodb.js using Mongoose to connect to MONGO_URI. Create config/sqlite.js using better-sqlite3 to initialize database/auditlog.db and create table `audit_logs` (id INTEGER PRIMARY KEY AUTOINCREMENT, method TEXT, route TEXT, userId TEXT, timestamp DATETIME DEFAULT CURRENT_TIMESTAMP) if it does not exist. Export connection instances.</action>
  <verify>node -e "require('./config/mongodb'); require('./config/sqlite')"</verify>
  <done>Database configuration scripts are implemented and can be loaded successfully.</done>
</task>

<task type="auto">
  <name>Implement running entry server.js</name>
  <files>server.js</files>
  <action>Create server.js to load environment variables, require config/mongodb.js and config/sqlite.js, start Express application listening on PORT.</action>
  <verify>node --check server.js</verify>
  <done>server.js is ready to run.</done>
</task>
</tasks>

<verification>
- [ ] npm install runs successfully without errors
- [ ] database/auditlog.db is created at runtime with correct schema
</verification>

<success_criteria>
- All setup files exist
- Server compiles and is ready to start
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-dual-database-connection/01-01-SUMMARY.md`
</output>
