---
phase: 01-project-setup-dual-database-connection
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [package.json, .env, .gitignore, config/mongodb.js, config/sqlite.js, server.js]
autonomous: true
requirements: [DB-01, DB-02, LOGG-01, DOCS-01]

must_haves:
  truths:
    - "Express server runs on local PORT"
    - "MongoDB connection via Mongoose is successful"
    - "SQLite auditlog.db is created and audit_logs table exists"
  artifacts:
    - path: "package.json"
      provides: "dependencies for express, mongoose, better-sqlite3, dotenv"
    - path: "config/mongodb.js"
      provides: "MongoDB connection logic"
    - path: "config/sqlite.js"
      provides: "better-sqlite3 connection and table creation"
    - path: "server.js"
      provides: "main application entry point"
---

<objective>
Configure the Node.js/Express environment, establish Mongoose connections to MongoDB, auto-create the SQLite database with its logs table, and run the server.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Project Initialization and Dependencies</name>
  <files>package.json, .env, .gitignore</files>
  <action>Initialize package.json, add start scripts, install express, mongoose, better-sqlite3, dotenv, jsonwebtoken, bcrypt. Create .env with standard keys. Create .gitignore ignoring node_modules, .env, and *.db files.</action>
  <verify>npm list express mongoose better-sqlite3 dotenv</verify>
  <done>package.json has dependencies and gitignore matches requirements</done>
</task>

<task type="auto">
  <name>Task 2: Configure MongoDB via Mongoose</name>
  <files>config/mongodb.js</files>
  <action>Write Mongoose connection logic reading MONGO_URI from .env, log connection status on success/error.</action>
  <verify>node -e "require('dotenv').config(); require('./config/mongodb.js')" logs success</verify>
  <done>MongoDB connection function works</done>
</task>

<task type="auto">
  <name>Task 3: Configure SQLite via better-sqlite3</name>
  <files>config/sqlite.js</files>
  <action>Write better-sqlite3 connection logic, auto-create database/ directory, and initialize the audit_logs table if not exists.</action>
  <verify>node -e "require('./config/sqlite.js')" creates database/auditlog.db and runs without error</verify>
  <done>SQLite DB and audit_logs table are created at runtime</done>
</task>

<task type="auto">
  <name>Task 4: Main Server Setup</name>
  <files>server.js</files>
  <action>Create Express app, import database configurations, set up simple health route, and start listening on PORT.</action>
  <verify>node server.js starts without crash</verify>
  <done>Server starts and database connections succeed</done>
</task>
</tasks>

<verification>
- [ ] npm install completes successfully
- [ ] Database files and connections initialized
- [ ] server.js runs without exceptions
</verification>

<success_criteria>
- All database connections succeed
- Environment variables are loaded
- Express server runs on PORT
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-dual-database-connection/01-01-SUMMARY.md`
</output>
